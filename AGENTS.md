# AGENTS.md — job-search-agent 工程协作规范

## 1. 项目概述

本项目名称为 `job-search-agent`。

这是一个面向 BOSS 直聘和脉脉的半自动求职辅助系统，主要功能包括：

- 管理求职策略；
- 读取和分析职位信息；
- 对职位进行规则化评分；
- 生成个性化招呼语；
- 根据用户真实工作经历生成招聘沟通回复；
- 管理网站内附件简历；
- 识别电话和面试邀请；
- 查询日历冲突；
- 将高风险操作提交给用户确认。

本文件只保留长期稳定的工程协作规范，不重复完整业务需求。详细规则参见：

- `docs/product-requirements.md`：产品目标、范围、功能和验收标准；
- `docs/scoring-rules.md`：JD 解析、硬性排除、100 分评分模型和等级规则；
- `docs/conversation-policy.md`：自动沟通、简历发送、敏感信息和人工确认规则；
- `docs/scheduling-policy.md`：电话沟通、面试时间、日历冲突和时间确认规则；
- `docs/development-plan.md`：开发阶段、实施顺序和阶段验收标准。

## 2. 规则优先级

出现冲突时按以下优先级处理：

1. 用户在当前任务中的明确要求；
2. 对应领域的 `docs/` 专项文档；
3. 本文件中的长期工程规范；
4. README、代码注释和其他辅助资料。

如果专项文档之间存在冲突，不得擅自选择有争议的实现，应记录冲突、说明影响并请求用户确认。

## 3. 工作语言

- 与用户交流时使用简体中文；
- 代码、变量名、类名和方法名使用英文；
- 代码注释优先使用中文，重点解释业务原因、约束和取舍，不机械复述代码；
- API 字段和数据库字段使用英文；
- Git 提交信息使用中文，保持简洁明确；未经用户明确要求不得自动提交。

## 4. 技术栈

### 4.1 后端

- Python 3.12
- FastAPI
- Pydantic v2
- SQLAlchemy 2
- PostgreSQL
- Alembic
- pytest

### 4.2 前端

- React
- TypeScript
- Ant Design
- Vite

### 4.3 浏览器自动化

- Playwright；
- 使用用户手动登录后的浏览器会话；
- 不保存招聘平台账号密码；
- 在 `docs/development-plan.md` 规定的阶段之前不得接入真实招聘网站。

## 5. 核心架构原则

系统采用清晰的模块化单体设计，至少包含以下领域模块：

- `job_parser`：职位信息提取和规范化；
- `scoring`：硬性过滤和职位评分；
- `policy_engine`：自动操作权限判断；
- `knowledge_base`：用户真实经历和技能知识库；
- `conversation_agent`：招聘消息分析和回复生成；
- `resume_selector`：简历选择和发送条件判断；
- `scheduling`：电话和面试时间处理；
- `browser_worker`：招聘网站浏览器操作；
- `audit`：决策和操作审计。

长期约束：

- 业务规则不得散落在浏览器页面脚本、API 路由或前端组件中；
- 浏览器适配器只负责读取页面和执行已批准的操作，不负责决定是否发送消息或简历；
- 外部招聘平台、日历和大模型通过适配器隔离，领域逻辑不得直接依赖具体供应商；
- 规则引擎负责硬性过滤、权限判断和最终计分；
- 所有自动操作在执行前必须经过策略引擎或权限判断；
- 涉及用户确认的权限必须由服务端校验，不得只依赖前端按钮；
- 原始外部数据和规范化数据分开保存，便于重新解析和审计；
- 关键规则结果必须可解释、可审计、可复现；
- 不为未来的分布式部署或未进入当前阶段的功能提前引入复杂架构。

## 6. 安全边界

禁止实现或协助实现：

- 验证码破解；
- 滑块绕过；
- 浏览器指纹伪装；
- 代理池轮换；
- 平台反检测或其他规避平台风控的功能；
- 未经授权抓取他人隐私数据；
- 无限制批量投递；
- 虚构用户工作经历、技能、项目、学历或业绩；
- 自动发送身份证、银行卡、家庭信息等敏感内容；
- 未经授权保存或输出密码、Cookie、Token、私钥和生产连接信息；
- 自动上传未知文件或绕过网站内附件限制。

遇到验证码、登录验证、设备验证、页面异常、目标身份不匹配或会话发生变化时，必须停止自动操作并通知用户。

## 7. 人工确认规则

以下操作必须经过用户确认：

- 电话沟通具体时间；
- 视频面试具体时间；
- 现场面试具体时间；
- 提出新的面试时间；
- 最终薪资承诺；
- 最低薪资底线；
- 确切到岗日期；
- 录用或入职承诺；
- 敏感信息回复；
- 低置信度回复；
- 无知识库事实支持的回复。

系统不得仅因为日历时间空闲就自动接受面试。具体确认范围和状态规则以 `docs/conversation-policy.md` 和 `docs/scheduling-policy.md` 为准。

## 8. 事实真实性规则

所有关于用户经历、技能、学历、项目和薪资的信息，必须来源于用户知识库或经过用户确认的配置，并记录必要的事实来源。

知识库不存在或来源不可靠的信息：

- 不允许推测；
- 不允许补全；
- 不允许为了提高职位匹配度而虚构；
- 不允许用于自动回复；
- 应进入人工确认流程。

大模型可以进行语义分析和文字生成，但不能作为用户事实来源。日志不得包含无必要的个人隐私或完整敏感信息，示例配置只能使用明显的占位值。

## 9. 评分原则

职位最终分数和硬性排除结果必须由确定性的程序规则计算。

大模型只允许用于：

- 解析 JD；
- 提取职位职责；
- 提取必须技能和优先技能；
- 判断受控范围内的语义相关性；
- 基于已验证事实生成匹配理由；
- 基于规则结果生成风险提示。

大模型不得直接决定或修改最终职位总分、等级、硬性排除结果，也不得绕过策略规则。

评分结果应保存策略版本、规则版本或必要输入快照，保证历史结果可以复现和解释。具体规则以 `docs/scoring-rules.md` 为准。

## 10. 浏览器操作原则

每次执行浏览器写操作前，必须重新核对：

- 平台；
- 公司；
- 职位；
- 招聘人；
- 当前会话；
- 发送内容；
- 简历名称；
- 操作是否已经批准；
- 操作是否已经执行过；
- 相关自动化开关是否仍然启用；
- 确认任务是否过期。

页面选择器失效、页面内容不一致、目标身份不匹配或当前会话发生变化时，必须停止操作。

所有浏览器写操作必须有结构化审计日志。发送消息和简历必须具备幂等性及去重保护；简历只能从网站内已经存在的附件中选择。

## 11. 开发工作方式

执行任务前：

1. 阅读本文件；
2. 阅读与任务相关的 `docs/` 文档；
3. 检查 Git 工作区、现有代码和测试；
4. 说明本次需求理解、假设和验收标准；
5. 对多步骤或高风险任务给出简短计划及预计修改文件；
6. 再进行代码修改。

除非用户明确要求只做设计，否则完成设计后应继续实施任务，不要停留在建议阶段。

实现时必须遵守：

- 只修改完成当前需求所需的最少代码和文档；
- 不擅自扩大任务范围；
- 不在同一任务中进行无关的大规模重构；
- 不顺手格式化、重命名、清理或优化无关代码；
- 沿用项目现有目录、命名、错误处理、日志和测试风格；
- 不为未进入当前阶段的功能创建无实际用途的空模块或抽象层；
- 新增依赖前先评估标准库和现有依赖，并说明新增理由和影响；
- 不吞异常，不遗留调试输出，不通过削弱断言让测试通过。

## 12. 代码质量要求

- 使用类型注解；
- 公共接口应有清晰的 Docstring；
- 函数和类保持职责单一，命名表达业务含义；
- 业务规则集中管理，避免魔法数字；
- 可变业务规则使用配置或数据模型表达，不得散落硬编码；
- 数据库变更必须创建 Alembic 迁移，不能只修改 ORM 模型；
- API 请求和响应使用 Pydantic 模型；
- 外部平台数据必须校验后才能进入领域逻辑；
- 金额、币种、时间、时区和状态应使用明确的结构化表示；
- 关键操作必须具备幂等性；
- 消息发送和简历发送必须防止重复执行；
- 本次修改产生的不再使用代码可以清理，但不得借机清理历史遗留内容。

## 13. 测试要求

每次修改后，根据涉及模块按风险由小到大运行验证：

1. 与修改最相关的单元测试；
2. API 或数据库集成测试；
3. 类型检查和 Lint；
4. 前端测试和生产构建；
5. 条件允许时运行完整测试集。

后端默认运行：

```bash
pytest
```

如已配置代码检查，还应运行：

```bash
ruff check .
mypy .
```

前端默认运行：

```bash
npm run lint
npm run test
npm run build
```

Playwright 适配器必须使用模拟页面或测试夹具完成基础测试，不允许单元测试直接操作真实招聘账号。

评分、策略、权限和发送类规则至少覆盖：

- 正常路径；
- 阈值边界；
- 非法输入；
- 规则冲突；
- 硬性排除或拒绝路径；
- 重复执行；
- 外部服务失败时的安全降级。

必须如实报告实际运行的命令、结果和未验证部分。未运行测试时不得声称测试通过。

## 14. Git 与工作区

- 修改前检查 Git 工作区；目录尚未初始化 Git 时应明确说明；
- 不覆盖、删除、回滚或提交用户已有的无关修改；
- 未经用户明确要求，不得自动创建或切换分支、提交、推送或修改 Git 配置；
- 提交范围必须与当前任务一致，不得夹带无关格式化或清理；
- 禁止使用可能破坏用户工作区的命令，除非用户明确授权并已核对目标。

## 15. 文档同步

以下变化必须同步更新对应文档：

- 产品范围和阶段验收标准；
- 数据库模型；
- API 接口；
- 求职评分规则；
- 自动回复策略；
- 人工确认范围；
- 浏览器适配方式；
- 环境变量；
- 启动和测试命令。

不得出现代码已经变更但文档仍描述旧行为的情况。业务规则应更新其对应的专项文档，不要把完整需求重新复制到本文件。

## 16. 完成报告

修改完成后必须报告：

- 完成了哪些内容；
- 创建或修改了哪些文件以及原因；
- 实现了哪些功能；
- 运行了哪些测试或检查；
- 测试或检查结果；
- 尚未完成、未验证或存在风险的内容。

如果任务未全部完成，应明确区分已完成内容、未完成内容和阻塞原因。
